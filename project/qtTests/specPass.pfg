//берем спец-элемент и передаем его в другую функцию, где и используем
//проверяется, нет ли ошибок с обработкой спец-элементов в других функциях

qtTests.passSpec << funcdef X
{
	return << (X,+):qtTests.openSpec;
}

qtTests.openSpec << funcdef X
{
	x1 << X:1;
	x2 << X:2;
	return << x1:x2;
}
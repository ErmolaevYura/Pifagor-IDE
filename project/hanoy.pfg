//==============================================================================
// Ханойские башни
//==============================================================================

//------------------------------------------------------------------------------
// � екурсивное решение
//------------------------------------------------------------------------------

//rHanoy << prefunc
// Функция, непосредственно, реализующая рекурсивное решение
rHanoy << funcdef X
  // где X - тройка, задающая: 
  //   (исходный_стержень, приемный стержень, количество_перемещаемых_дисков)
  /* Функция возвращает список пар: ((исходный стержень, приемный стержень), ...)
  // порядок пар определяет последовтельность перекладываний дисков*/
  // со стержня на стержень
{ 
  src << X:1;
  dst << X:2;
  n   << X:3;
  return << .^
  /*Проверка на окончание решения
  */[(n,0):(<=, >):?]^
  // Выбираемые варианты
  (
    {.},
    {
      ( 
      (src, ((3,src):-,dst):-, (n,1):-):rHanoy:[], 
      (src, dst), 
      (((3,src):-,dst):-, dst, (n,1):-):rHanoy:[]
      )
    }
  )
};

// Функция, задающая точку входа в рекурсивное решение
RecHanoy << funcdef n
  // n - целое число, задающее общее количество дисков
  // Функция возвращает список пар: ((исходный стержень, приемный стержень), ...)
  // порядок пар определяет последовтельность перекладываний дисков
  // со стержня на стержень
{                
  // Запуск функции, непосредственно реализующей рекурсивное решение
  (0, 2, n):rHanoy:[] >> return
};
